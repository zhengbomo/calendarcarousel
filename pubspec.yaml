name: calendar_carousel
description: A lightweight and highly customizable calendar view for your Flutter app.
version: 0.0.1
author: zhengbomo <zhengbomo@Hotmail.com>
homepage: https://github.com/zhengbomo/calendarcarousel

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.15.8


dev_dependencies:
  flutter_test:
    sdk: flutter

# For information on the generic Dart part of this file, see the
# following page: https://www.dartlang.org/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The androidPackage and pluginClass identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    androidPackage: com.bomo.calendar_carousel
    pluginClass: CalendarCarouselPlugin